// Custom step definitions for Hop BDD Framework
// Add your custom step definitions here

export default {
  // Example custom step:
  // 'Given I wait for {int} seconds': async (step, context) => {
  //   const seconds = parseInt(step.text.match(/\d+/)?.[0] || '0');
  //   await new Promise(resolve => setTimeout(resolve, seconds * 1000));
  // },
};
